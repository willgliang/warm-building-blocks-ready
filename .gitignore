node_modules
dist
dist-ssr
*.local
.env
.env.local
.DS_Store

# Ignore stale JS/JSX compiled files that shadow TS/TSX sources
# (Lovable generates these locally; they break Vite resolution order)
src/**/*.js
src/**/*.jsx
!src/vite-env.d.ts
